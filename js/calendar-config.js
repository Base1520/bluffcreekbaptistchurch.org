/* Public configuration only. Set the approved read-only endpoint after backend activation.
   Never place the original iCloud feed, Apple credentials or a secret key here. */
var CREEK_PUBLIC_CALENDAR = {
  endpoint: ""
};
