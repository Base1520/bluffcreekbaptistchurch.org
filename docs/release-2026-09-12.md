# September 12 website release

This release keeps the approved Homestead design, removes the former children’s ministry staff listing, and routes those questions to the church office. Giving copy directs receipt questions to the office without promising an unverified statement workflow.

The curated calendar snapshot is the reviewed September 11 import: 95 occurrences through December 9. JSON, embedded fallback and calendar download agree. Only snapshot data changed; the existing consumer logic and empty automatic-refresh configuration are preserved. This is a dated calendar copy, not an automatic iCloud subscription.

Validation before publication: 21 Python tests and 28 Node tests pass. The independent local checker passes 545 local references, 27 fragments, 14 legacy redirects, 12 main pages, 27 HTML documents and the 11-URL canonical sitemap. Desktop and 390px phone layouts were reviewed; the menu opens and Plan a Visit navigates correctly without horizontal overflow or broken loaded images. The app, public event file and church-named Stripe checkout returned HTTP 200. No payment or contact submission was made. A real broadcast is a separate streaming acceptance task.

Publish through PR2, then verify the repository URL. Configure www.bluffcreekbaptistchurch.org as the canonical Pages domain and update only the website DNS records. Preserve app and mail records, nameservers and the Squarespace account. Verify HTTPS, root-to-www forwarding, current served file hashes and legacy routes before recording the domain launch as complete.

PR3 duplicates the Social Studio already incorporated into PR2; do not merge it independently. Future builds must preserve CNAME and the updated staff source. Calendar upkeep, third-party checkout wording, analytics and physical-phone acceptance remain separate operator tasks.
